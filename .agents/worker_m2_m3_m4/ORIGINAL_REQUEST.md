## 2026-06-17T01:56:40Z
Implement the onboarding flow improvements, deep data integration, and UI/UX contrast fixes in the GoGreen application.

MANDATORY INTEGRITY WARNING:
DO NOT CHEAT. All implementations must be genuine. DO NOT hardcode test results, create dummy/facade implementations, or circumvent the intended task. A Forensic Auditor will independently verify your work. Integrity violations WILL be detected and your work WILL be rejected.

Please execute the following tasks:

1. Integrate `src/app/page.tsx` with the refactored calculations in `src/utils/karmaLogic.ts`:
   - Import types, `defaultProfile`, `calculateMonthlyLeak`, `createActions`, `getStoryCards`, `totalsByCategory`, `formatPoints`, `formatRupees`, and `carbon` from `@/utils/karmaLogic` inside `src/app/page.tsx`.
   - Remove duplicate definitions of those types, defaults, and helper functions from `src/app/page.tsx`.
   
2. Polish Onboarding Flow (`SetupOverlay` component in `src/app/page.tsx`):
   - Update `SetupOverlay` to be 4 steps instead of 3.
   - Insert step index 2: "Baseline Details". In this step, collect Household size (stepper), Monthly electricity bill (Rupees range), and Weekly commute distance (km range) from the user using inputs matching the styling of `OnboardingCard`.
   - Change Step 3 to Motivation selection.
   - Make sure that when the overlay finishes, the state is correctly updated with all collected inputs.
   - Correct default profile in `page.tsx` or imported to have `bill` at `1500` (which is within the range limits of the bill slider [800, 8000]).
   - Fix all bill formatting displays in `SetupOverlay`, `ProfileView` and `OnboardingCard` to use `formatRupees` instead of `formatPoints`.

3. Rewire Data Integration and Dashboard Metrics:
   - Make sure that the actions generated by `createActions` are strictly driven by the profile values collected during setup.
   - Scale down and suppress irrelevant actions (e.g. do not show AC optimization if `acHours: 0`, do not show commute swap if `commuteKm: 0` or commute mode is walk/bike).
   - In `TodayView` in `src/app/page.tsx`, calculate `monthlyLeak = calculateMonthlyLeak(profile)`.
   - Add a new Metric Card displaying "Monthly lifestyle leak" with the currency-formatted value (e.g., `₹2,940`) in the main dashboard grid.
   - Wrap this Metric Card or place it inside a container with `data-testid="monthly-leak"` to ensure programmatic verifiability.
   - Update `RecapView`'s "Money avoided" stat card to display value formatted with `formatRupees`.

4. Fix UI/UX Contrast & Styling:
   - Edit `src/app/globals.css` and add the theme override rules for `.theme-dark-panel` to ensure that when light mode is enabled (`.theme-light`), elements wrapped in `.theme-dark-panel` retain their white text and correct dark borders/backgrounds/input colors.
   - In `src/app/page.tsx`, add the class `theme-dark-panel` to:
     - The onboarding "WHY" card wrapper container.
     - The absolute container for the text overlays on the story card images.
     - The outer card container of the `SetupOverlay` modal.
   - Ensure the dropdown option element background colors in light mode do not bleed or cause contrast breaks.

5. Verification:
   - Run compilation and tests for `scripts/verify-karma.ts`:
     - Compile: `npx tsc scripts/verify-karma.ts --target es2022 --module commonjs --moduleResolution node`
     - Run: `node scripts/verify-karma.js`
   - Run `npm run build` to verify the codebase builds without typescript or compilation errors.
   - Document the build and test outputs in your handoff report.
